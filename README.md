# ScriptDock

A boring desktop launcher that finds the `run-dev` / `run-built` / `rebuild` scripts scattered across your projects and runs and reliably restarts them as processes it owns — so you stop hunting through Finder for them and stop drowning in idle terminal tabs.
